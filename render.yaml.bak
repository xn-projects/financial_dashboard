
services:
  - type: web
    name: financial-dashboard
    env: python
    plan: free
    buildCommand: 'pip install -r requirements.txt'
    startCommand: 'uvicorn app.app:api --host=0.0.0.0 --port=$PORT'
